services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4321:4321"
    volumes:
      - app-data:/app/data
    environment:
      - HOST=0.0.0.0
      - PORT=4321
      - NODE_ENV=production

volumes:
  app-data:
