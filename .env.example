# GitHub
GITHUB_WEBHOOK_SECRET=       # Secret pour valider les webhooks
GITHUB_TOKEN=                # Token pour clone/push
GITHUB_SOURCE_REPO=          # Repo Lovable (owner/repo)
GITHUB_TARGET_REPO=          # Repo Astro output (owner/repo)

# Coolify
COOLIFY_API_URL=             # URL de l'API Coolify (ex: https://coolify.example.com)
COOLIFY_API_TOKEN=           # Token API Coolify
COOLIFY_PREVIEW_APP_ID=      # ID de l'app preview sur Coolify
COOLIFY_PROD_APP_ID=         # ID de l'app prod sur Coolify

# Auth
SESSION_SECRET=              # Secret pour signer les cookies de session (openssl rand -hex 32)

# Brevo (email)
BREVO_API_KEY=               # Clé API Brevo pour l'envoi d'emails
FROM_EMAIL=noreply@simplauto.com  # Adresse expéditeur
